import { HighlightArrayItemDirective } from './highlight-array-item.directive';

describe('HighlightArrayItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightArrayItemDirective();
    expect(directive).toBeTruthy();
  });
});
