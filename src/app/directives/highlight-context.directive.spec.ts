import { HighlightContextDirective } from './highlight-context.directive';

describe('HighlightContextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightContextDirective();
    expect(directive).toBeTruthy();
  });
});
